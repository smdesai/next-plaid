# Rust build output
/target/

# xcframework-build.sh artifacts
/build/

# UniFFI-generated bindings (regenerated by xcframework-build.sh; the copy that
# ships lives in the Swift package, not here)
/generated/

# Cargo.lock IS tracked: this standalone crate produces staticlib/XCFramework
# artifacts, so a pinned lockfile keeps builds reproducible.
